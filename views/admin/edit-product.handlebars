<body>
  <main>

    {{#if errorMessage}}
      <div class="user-message user-message--error">
        {{errorMessage}}
      </div>
    {{/if}}
    <form class="product-form" action="/admin/edit-product" method="POST">
      <div class="form-control">
        <label for="title">
          Title
        </label>
        <input
        class="{{#if validationStyles.title}} invalid{{/if}}"
        id="title"
        type="text"
        name="title"
        value="{{product.title}}" />
      </div>
      <div class="form-control">
        <label for="price">
          Price
        </label>
        <input
           class="{{#if validationStyles.price}} invalid{{/if}}"
          id="price"
          type="number"
          name="price"
          step="0.01"
          value="{{product.price}}"
        />
      </div>
      <div class="form-control">
        <label for="imageURL">
          Image
        </label>
        <input
           class="{{#if validationStyles.imageURL}} invalid{{/if}}"
          id="image"
          type="text"
          name="imageURL"
          value="{{product.imageURL}}"
        />
      </div>
      <div class="form-control">
        <label for="description">
          Description
        </label>
        <textarea
        class="{{#if validationStyles.description}} invalid{{/if}}"
        id="description"
        type="text"
        name="description"
        rows="5">
          {{product.description}}
        </textarea>
      </div>
      <input type="hidden" value={{product._id}} name="productId" />
      <input type="hidden" name="_csrf" value={{csrfToken}} />
      <button class="submit-button" type="submit">
        Submit changes
      </button>
    </form>
  </main>
</body>